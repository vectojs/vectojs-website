+++
title = "수학적 기초"
description = "VectoJS 렌더링 엔진의 기반이 되는 수학적 및 물리적 원리: Virtual Math Trees, 시맨틱 접근성 프로젝션, Lie 그룹, 분할 레이아웃, 스플라인 히트 테스팅, 집합-차분 래핑, ODE 애니메이션, 공간 컬링."
weight = 6
+++

# 수학적 기초

VectoJS는 UI 렌더링을 일련의 CSS 스타일링 캐스케이드 해결이 아닌 순수한 **기하 및 대수 연산 문제**로 취급합니다. 레이아웃, 컬링, 히트 테스팅, 텍스트 래핑, 애니메이션을 형식적 수학 시스템으로 변환함으로써 엔진은 브라우저의 DOM 및 레이아웃 재계산 파이프라인을 완전히 우회합니다.

이 문서는 VectoJS 런타임의 기초가 되는 여덟 가지 수학 및 엔지니어링 기둥을 자세히 설명합니다.

---

## 1. Virtual Math Tree (VMT)

VectoJS는 무거운 브라우저 DOM 노드 트리를 유지하는 대신 **Virtual Math Tree (VMT)** 에서 작동합니다. VMT는 마크업 요소의 표현이 아닌 순수한 인-메모리 **대수 및 좌표 씬 그래프**입니다.

### 순수-메모리 대수 표현

전통적인 UI에서 레이아웃은 브라우저의 리플로우 엔진에 의해 해결되며, 이는 캐스케이딩 박스 모델을 계산하고 CSS 렌더 레이어를 업데이트합니다. VMT에서 모든 시각적 요소(Entity)는 지역화된 좌표계로 표현되며, 아핀 대수 관계를 통해 부모에 매핑됩니다:

$$\\mathbf{M}\_{\\text{world, child}} = \\mathbf{M}\_{\\text{world, parent}} \\cdot \\mathbf{M}\_{\\text{local}}$$

시각적 트리는 그리기 가능한 요소당 하나의 스타일이 적용된 HTML 노드를 필요로 하지 않습니다. 렌더 트래버설은 JavaScript에서 숫자 변환을 합성합니다; 접근성 동기화 및 DOM 포털은 별도의 브라우저-대면 단계로, 그 비용은 여전히 측정되어야 합니다.

### 할당-인식 핫 경로

높은 렌더링 처리량을 유지하기 위해 VMT의 렌더 워크는 트래버설 중 노드별 할당을 피합니다.

- **스레드된 스칼라 변환**: 노드당 행렬 객체를 할당하는 대신 렌더 워크는 여섯 개의 스칼라 변환 매개변수를 재귀를 통해 직접 스레드하여 프레임당 노드별 힙 할당을 피합니다.
- **텍스트용 플랫 스칼라 배열**: `LayoutResultBuffer`는 레이아웃 좌표를 미리 할당된 연속적인 TypedArray에 패킹하여 프레임 간에 재사용할 수 있게 하여 레이아웃 노드 할당 압력을 줄입니다. 이는 전체 Scene이나 호출자가 제로-할당이 된다는 것을 의미하지는 않습니다.

이것이 의미하지 _않는_ 것: 렌더 워크, 히트 테스팅, 접근성-동기화 패스가 실행될 때 여전히 $O(N)$입니다. `renderMode: 'onDemand'`는 정적 프레임에 대해 그리기 및 엔티티 트래버설을 건너뛰지만, Scene은 rAF를 계속 폴링하고 더티/애니메이션 상태를 확인합니다.

---

## 2. 시맨틱 Shadow DOM (a11yRoot)

캔버스 기반 UI 엔진은 역사적으로 "블랙 박스" 결함으로 고통받아 왔습니다: 스크린 리더에 완전히 보이지 않고, 자동화된 접근성 엔진이 감사할 수 없으며, 복사-붙여넣기나 Input Method Editor(CJK IME) 구성과 같은 네이티브 브라우저 기능이 작동하지 않습니다.

VectoJS는 **시맨틱 Shadow DOM**(또는 `a11yRoot`)을 통해 이를 해결합니다.

### 능동적 접근성 프로젝션

VectoJS가 모든 그래픽을 단일 `<canvas>` 요소 내부에서 직접 렌더링하는 동안, 캔버스 좌표 공간 바로 위에 절대 위치로 중첩된 보이지 않는 고정밀 **시맨틱 Shadow DOM**을 유지합니다.

```text
┌────────────────────────────────────────────────────────┐
│  Semantic Shadow DOM (a11yRoot: <button>, <input>...)  │  <-- Interactive/A11y Layer
├────────────────────────────────────────────────────────┤
│  WebGL / Canvas 2D Graphics Canvas Layer               │  <-- High-Performance Graphics
└────────────────────────────────────────────────────────┘
```

Virtual Math Tree의 모든 인터랙티브 엔티티에 대해 엔진은 해당 시맨틱 HTML 요소(예: 버튼은 `<button>`, 입력 필드는 `<input>`, 링크는 `<a>`)를 shadow root에 프로젝션합니다. 이 DOM 노드는 완전히 투명하지만 해당 Canvas 요소의 물리적 경계, 중첩 순서 및 인터랙티브 상태와 일치합니다.

### 테스팅 및 스크린 리더 무결성

shadow DOM이 표준 네이티브 HTML 태그로 구성되어 있기 때문에:

- **스크린 리더**: 접근성 도구가 네이티브 시맨틱 태그와 상호작용하여 설명을 읽고 상태를 파악합니다.
- **자동화된 테스팅**: Playwright나 AI Agent와 같은 프레임워크가 `page.getByRole('button', { name: 'Submit' })` 같은 표준 쿼리 메커니즘을 사용하여 캔버스 기반 UI 컴포넌트를 찾고 쿼리할 수 있습니다.
- **IME 구성**: CJK 입력 방식이 shadow 레이어 내부의 실제 `<input>` 태그와 상호작용하기 때문에 네이티브로 작동하며, 이는 실시간으로 VectoJS의 렌더링 엔진에 구성된 문자열을 스트리밍합니다.

---

## 3. 아핀 변환

VectoJS는 `absolute`, `relative`, `flex` 포지셔닝과 같은 CSS 레이아웃 속성을 완전히 버립니다. 대신 Virtual Math Tree의 모든 노드의 공간 관계는 유클리드 평면에서 $3 \\times 3$ 동차 **아핀 변환 행렬**로 압축됩니다.

### 변환 행렬

엔티티의 이동 $(t_x, t_y)$, 스케일 $(s_x, s_y)$, 회전 $\\theta$ (라디안)은 단일 행렬 $M \\in \\text{Aff}(2)$로 결합됩니다:

VectoJS는 로컬 변환을 $T \\cdot S \\cdot R$로 적용합니다:

$$M = \\begin{bmatrix} s_x \\cos\\theta & -s_x \\sin\\theta & t_x \\\\\\\\ s_y \\sin\\theta & s_y \\cos\\theta & t_y \\\\\\\\ 0 & 0 & 1 \\end{bmatrix}$$

이동과 회전만으로는 강체 운동 그룹 $SE(2)$를 구성하며; 스케일(및 중첩된 비균일 스케일과 회전에 의해 생성된 전단)을 추가하려면 더 일반적인 아핀 그룹이 필요합니다.

### 캐스케이딩 변환 (행렬 곱셈)

노드 트리를 탐색할 때 자식은 부모의 좌표 공간을 상속받습니다. 행렬 곱셈은 결합 법칙을 따르므로 중첩된 엔티티의 전역 변환 행렬 $M_{\\text{global}}$은 부모의 누적된 전역 행렬과 로컬 행렬을 곱하여 계산됩니다:

$$M_{\\text{global}} = M_{\\text{parent}} \\times M_{\\text{local}}$$

이는 전위-순서 깊이 우선 탐색(DFS) 렌더 패스 중에 실행됩니다. VectoJS는 스칼라 float 변수에서 직접 이를 수행하여(힙 할당 방지) 계산 처리량을 최적화합니다:

```typescript
// Scalar multiplication of 3x3 transformation matrix
const globalX = parent.m00 * local.x + parent.m01 * local.y + parent.m02;
const globalY = parent.m10 * local.x + parent.m11 * local.y + parent.m12;
```

### 폐쇄형 역변환 (Cramer의 법칙)

좌표를 역매핑하기 위해(예: 화면-스페이스 마우스 클릭이나 3D 레이캐스트 좌표를 로컬 엔티티 좌표 공간으로 변환), VectoJS는 역행렬 $M_{\\text{global}}^{-1}$을 계산합니다.

VectoJS는 일반 행렬 솔버 대신 여섯-스칼라 아핀 행렬의 폐쇄형 역변환을 사용합니다:

$$M^{-1} = \\frac{1}{\\det(M)} \\begin{bmatrix} m_{11}m_{22} - m_{12}m_{21} & m_{02}m_{21} - m_{01}m_{22} & m_{01}m_{12} - m_{02}m_{11} \\\\\\\\ m_{12}m_{20} - m_{10}m_{22} & m_{00}m_{22} - m_{02}m_{20} & m_{02}m_{10} - m_{00}m_{12} \\\\\\\\ m_{10}m_{21} - m_{11}m_{20} & m_{01}m_{20} - m_{00}m_{21} & m_{00}m_{11} - m_{01}m_{10} \\end{bmatrix}$$

동차 행렬의 세 번째 행이 항상 $\\begin{bmatrix} 0 & 0 & 1 \\end{bmatrix}$이므로, 행렬식은 다음과 같이 줄어듭니다:

$$\\det(M) = m_{00} \\cdot m_{11} - m_{01} \\cdot m_{10}$$

$\\det(M) \\neq 0$이면 `worldToLocal()`이 상수 산술 시간에 역좌표를 풀어 `{ x, y }` 포인트를 반환합니다. 특이 변환은 `null`을 반환합니다.

---

## 4. Cold/Hot 분할 레이아웃 엔진

웹에서 텍스트 렌더링은 악명 높게 느립니다. 전통적인 브라우저에서는 단일 문자를 수정해도 문서 전체에 걸쳐 대규모 리플로우(너비 재계산, 단어 분할, OS-레벨 폰트 캐시 쿼리)가 트리거될 수 있습니다. VectoJS는 **Cold/Hot 분할 레이아웃 엔진**을 통해 이를 해결합니다.

### Cold 경로: 분할 및 측정

텍스트 처리의 비용이 많이 드는 측면—토큰화(`Intl.Segmenter`를 사용한 단어 경계), BiDi (양방향 텍스트) 정렬, 캔버스 컨텍스트를 사용한 글리프 경계 측정—은 **Cold Pass**로 분리됩니다.

- 실제 텍스트 내용이 변경될 **때에만** 실행됩니다.
- 플랫하고 캐시 가능한 레이아웃 맵을 측정하고 구성합니다.
- 결과는 지역화된 불변 `PreparedText` 표현 내에 저장됩니다.

### Hot 패스: 수학-전용 래핑 및 정렬

텍스트 블록 크기가 조정될 때(예: 창 드래그 또는 반응형 레이아웃 카드 애니메이션), VectoJS는 **Hot Pass**를 트리거합니다:

- 모든 `Intl.Segmenter` 또는 캔버스 측정 API 쿼리를 피합니다.
- `PreparedText` 맵에서 직접 캐시된 글리프 너비를 읽습니다.
- 캐시된 어드밴스에 대한 숫자 연산으로 줄 바꿈, 수직 래핑 오프셋 및 문단 여백을 계산합니다.

콜드 준비를 핫 포지셔닝에서 분리하면 너비 변경이 분할 및 측정을 재사용할 수 있습니다. 문단 메모이제이션은 추가 작업 중 변경되지 않은 선행 문단도 재사용합니다. 변경된 문단은 여전히 자체 길이에 비례하며, Markdown 렉싱은 문서-전체로 유지됩니다.

---

## 5. 텍스트 흐름을 위한 집합-차분 대수

전통적인 웹 브라우저는 복잡한 CSS 레이아웃 계산을 사용하여 장애물(예: float 이미지나 인라인 콜아웃) 주위로 텍스트를 감쌉니다. VectoJS는 실수선에 대한 **구간 뺄셈 집합 이론**으로 텍스트 흐름을 수학적으로 모델링합니다.

### 라인-구간 분할

수직 좌표 $Y$와 높이 $H$의 주어진 줄에 대해 총 래핑 너비는 단일 닫힌 구간 $I_0 = [0, \\text{maxWidth}]$로 표현됩니다.

$K$개의 장애물 형상(`ExclusionRect`)이 Y-범위 $[Y, Y+H]$와 겹치는 경우, 각 장애물은 차감 구간 $E_k = [x_{s,k}, x_{e,k}]$를 나타냅니다:

<img src="/images/set-difference-intervals.svg" alt="세 개의 수평 구간 막대를 보여주는 다이어그램: 0에서 maxWidth까지의 전체 라인 구간, 중간의 장애물 구간 xs1에서 xe1, 그리고 그 결과로 장애물을 피하는 두 개의 개별 세그먼트로 구성된 집합 차분" class="diagram" />

텍스트 글리프를 배치하기 위한 사용 가능한 세그먼트 $I_{\\text{allowed}}$는 집합 차분을 계산하여 구합니다:

$$I_{\\text{allowed}} = I_0 \\setminus \\bigcup_{k=1}^{K} E_k$$

### 알고리즘 실행

엔진은 이 집합-차분 산술을 실행합니다:

1. Y-스팬과 겹치는 모든 제외 구간을 수집합니다.
2. 겹치는 제외 구간을 정렬된 분리 구간 목록으로 병합합니다.
3. 이 구간들을 $[0, \\text{maxWidth}]$에서 차감하여 유효한 하위-구간 목록을 생성합니다.
4. 텍스트 토큰을 순차적으로 이 하위-구간에 래핑합니다.

이를 통해 복잡한 타이포그래피 래핑을 재귀적 시행착오 렌더링 대신 결정론적 플랫 구간-차감 패스로 해결할 수 있습니다.

---

## 6. 샘플링된 스플라인 히트 테스팅

전통적인 캔버스 프레임워크는 곡선을 보이지 않게 그리고 색상 픽셀을 읽거나, 클릭이 곡선을 둘러싼 직사각형 경계 상자(AABB) 내부에 있는지 확인하여 히트 테스트합니다. 전자는 느리고 후자는 매우 부정확합니다.

`SplineEntity`는 각 3차 세그먼트를 $t \\in [0, 1]$에 대한 베지어 곡선 $P(t)$로 표현합니다:

$$P(t) = (1-t)^3 P_0 + 3(1-t)^2 t P_1 + 3(1-t) t^2 P_2 + t^3 P_3$$

여기서 $P_0, P_1, P_2, P_3 \\in \\mathbb{R}^2$는 제어점입니다.

### 폴리라인 근사

현재 구현은 각 베지어 세그먼트를 고정 해상도의 `Float32Array` 폴리라인으로 샘플링하고 캐싱합니다. 포인터 $C(x, y)$에 대해 각 인접 선분까지의 제곱 거리를 계산하고 다음 조건을 만족할 때 히트를 수락합니다:

$$d^2(C, \\overline{P*iP*{i+1}}) \\le \\left(\\frac{\\text{lineWidth}}{2} + \\text{hitTolerance}\\right)^2$$

캐시된 근사는 반복된 히트를 저렴하고 결정론적으로 만듭니다. 이는 해석적 5차/Newton 솔버가 아닙니다: 매우 높은 곡률의 세그먼트는 샘플 간에 편차가 발생할 수 있으므로 그 근사를 염두에 두고 `hitTolerance`를 선택하세요. `hitTest: 'aabb'`는 정제 과정을 완전히 건너뜁니다.

---

## 7. 미분 방정식 및 Semi-Implicit Euler 솔버

CSS 애니메이션은 고정된 시간 척도($t \\in [0, 1]$)에서 작동합니다. 목표 위치가 도중에 변경되면(예: 커서 추적), 베지어 곡선을 다시 계산해야 하므로 시각적 점프나 운동량 손실이 발생합니다.

VectoJS는 물리적 질량-스프링-댐퍼 시스템을 시뮬레이션하는 **상미분 방정식(ODE)** 을 사용하여 이 문제를 해결합니다.

### 지배 방정식

애니메이션 값 $x(t)$(위치, 스케일, 불투명도)가 목표 값 $x_{\\text{target}}$을 향한 움직임은 감쇠가 있는 Hooke의 법칙에 의해 제어됩니다:

$$m \\frac{d^2x}{dt^2} + c \\frac{dx}{dt} + k(x - x_{\\text{target}}) = 0$$

여기서:

- $m$은 질량(관성)입니다.
- $c$는 감쇠 계수(마찰)입니다.
- $k$는 스프링 강성(인력)입니다.

### 수치 적분 (Semi-Implicit Euler)

이 방정식을 런타임에 단계별로 풀기 위해 VectoJS는 **Semi-implicit Euler 적분** 솔버를 사용합니다. 명시적 Euler(불안정하고 에너지 오차가 누적됨)와 달리, semi-implicit 솔버는 현재 상태를 사용하여 속도를 계산한 다음 _다음_ 속도를 사용하여 위치를 계산합니다:

$$v_{t+\\Delta t} = v_t + \\frac{-k(x_t - x_{\\text{target}}) - c v_t}{m} \\Delta t$$

$$x_{t+\\Delta t} = x_t + v_{t+\\Delta t} \\Delta t$$

여기서 $\\Delta t$는 프레임 시간 간격(초)입니다.

솔버가 $x_{\\text{target}}$에 대한 현재 값 $x_t$와 속도 $v_t$를 기반으로 동적으로 힘을 계산하기 때문에 목표가 동적으로 움직일 수 있습니다(예: 드래그, 마우스 호버, 반응형 리플로우). 시스템은 자연스럽게 적응하며 운동량을 보존하고 유기적인 감쇠로 요소를 부드럽게 정지 상태로 만듭니다.

---

## 8. SpatialHashGrid 유틸리티

$N$개의 엔티티가 포함된 씬에서 내장된 히트 테스팅 및 뷰포트-컬링 트래버설은 $O(N)$입니다. 이것이 문제가 되는지는 엔티티 작업 및 대상 하드웨어에 따라 다릅니다.

VectoJS는 애플리케이션이 명시적으로 채워서 로컬 AABB 쿼리 비용을 제한할 수 있는 **SpatialHashGrid**를 내보냅니다. Scene의 내장 `findEntityAt()` 및 뷰포트 컬링은 이를 자동으로 사용하지 않으며, 둘 다 여전히 엔티티 트리를 탐색합니다.

<figure>
  <iframe src="/sandbox/diagram-spatial-hash.html" class="diagram-frame" loading="lazy" title="움직이는 커서가 자체 셀과 여덟 개의 이웃 셀만 테스트하는 공간 해시 그리드, VectoJS로 라이브 렌더링됨" sandbox="allow-scripts allow-same-origin"></iframe>
  <figcaption>공간-그리드 애플리케이션은 로컬 포인터/AABB 영역과 겹치는 셀만 쿼리할 수 있습니다. <em>(VectoJS로 라이브 렌더링된 개념.)</em></figcaption>
</figure>

### 해시 함수

좌표 공간은 크기 $S$의 셀로 구성된 그리드로 나뉩니다. 모든 엔티티의 경계 상자는 정수 셀 좌표 $(i, j) \\in \\mathbb{Z}^2$ 집합에 매핑됩니다:

$$i = \\left\\lfloor \\frac{x}{S} \\right\\rfloor, \\quad j = \\left\\lfloor \\frac{y}{S} \\right\\rfloor$$

이 그리드 좌표는 [Cantor 페어링 함수](https://en.wikipedia.org/wiki/Pairing_function)를 사용하여 단일 1D 버킷 키로 매핑되며, 음수 좌표는 먼저 비-음수 도메인으로 접힙니다:

$$x = \\begin{cases} 2i & i \\geq 0 \\\\\\\\ -2i - 1 & i < 0 \\end{cases} \\qquad y = \\begin{cases} 2j & j \\geq 0 \\\\\\\\ -2j - 1 & j < 0 \\end{cases}$$

$$H(i, j) = \\frac{(x + y)(x + y + 1)}{2} + y$$

버킷은 일반 `Map`에 존재하며 고정 용량 테이블이 아닙니다 — 모듈로 연산이 없으며, 맵은 존재하는 고유 점유 셀 수만큼 커집니다.

### 복잡도 감소

- **애플리케이션-관리 인덱싱**: 엔티티가 이동할 때 `insert(id, x, y, w, h)`를 호출하거나 동적 프레임을 위해 `clear()` 후 재구축합니다.
- **AABB 쿼리**: `query(x, y, w, h)`는 해당 박스와 겹치는 모든 그리드 셀을 방문하고 중복 제거된 `Set<string>`을 반환합니다.
- _결과_: 쿼리 시간은 겹친 셀 수 + 반환된 ID에 비례합니다. 작고 비슷한 크기이며 균일하게 분포된 엔티티에 대해서만 거의 상수에 가깝고, 밀집된 버킷은 선형 스캔으로 성능이 저하됩니다. [성능 가이드](/learn/performance/#3-엔티티-상호작용의-바다-on2-복잡성-문제)를 참조하세요.

---

## 수학적 이점 요약

| 차원            | 브라우저 / DOM           | VectoJS                  | 수학 원리             |
| --------------- | ------------------------ | ------------------------ | --------------------- |
| **씬 그래프**   | HTML DOM 트리            | Virtual Math Tree (VMT)  | 연속 메모리 씬 그래프 |
| **접근성**      | 네이티브 시맨틱 DOM      | 시맨틱 프로젝션 오버레이 | DOM 상태 동기화       |
| **변환**        | CSS 변환/레이아웃        | 3x3 동차 행렬            | 아핀 선형 대수        |
| **리플로우**    | 단일-스레드 리플로우     | Cold/Hot 분할 레이아웃   | 캐시된 단어 분할      |
| **텍스트 래핑** | 리플로우 시행착오        | 구간 뺄셈                | 집합 차분 대수        |
| **곡선 선택**   | 픽셀 읽기 / 광범위 AABB  | 캐시된 샘플링된 폴리라인 | 점-선분 거리          |
| **애니메이션**  | 3차 베지어 타임라인      | 질량-스프링-댐퍼 솔버    | 2차 ODE 적분          |
| **컬링**        | 브라우저 렌더링 휴리스틱 | 선택적 로컬 AABB 경계    | 변환된 AABB 겹침      |

> **다음:** [시작하기](/learn/getting-started/) — 패키지를 설치하고 첫 번째 씬을 작성하세요.
