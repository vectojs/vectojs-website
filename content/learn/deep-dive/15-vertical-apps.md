+++
title = "15 — Vertical Apps — Knowledge Graph, Node Editor, Desktop & Table"
description = "How vertical packages compose engine primitives — knowledge-graph over graph3d/force layout, node-editor commands and history, desktop window management, and table virtualization — and where app traps differ from engine defects."
weight = 35
+++

# 15 — Vertical Apps — Knowledge Graph, Node Editor, Desktop & Table

> Engine primitives are correct in isolation; verticals prove they compose under paging, undo, windowing, and 100k-row pressure. A table that only works at 10 rows, a graph that teleports on expand, or a window that leaks its overlay a11y mirror are all app-level composition bugs, not physics or renderer bugs — and the forge keeps them separate for a reason.

- **What you'll learn**: how four verticals compose stable primitives — `KnowledgeGraphModel` over `GraphLayout`/`Graph3D`, `NodeEditor` over `CommandHistory`/`SelectionState`/`layoutDocument`, `DesktopShell`/`WindowManager`/`DesktopWindow` over `Scene` overlays, and `Table` over `Text` + `GridCellHotspot` virtualization — with every file boundary and ownership rule that makes incremental growth cheap and teardown clean.
- **What you won't**: the physics inside `ForceLayout2D`/`VectoForceLayout` (boss 11), the VMT dirty lifecycle (boss 06), or renderer/DPR contracts (boss 07). This doc shows how apps _consume_ those engines, not how the engines compute.

## 1. Knowledge Graph — a paginated cut over 3D

### 1.1 Data contract

`KgEntity extends GraphNode` at `packages/knowledge-graph/src/types.ts:19` and `KgFact extends GraphLink` at `types.ts:31` so the same objects flow straight into `@vectojs/graph3d` layouts and renderers with domain fields untouched (`type`, `labels: LabelMap`, `predicate`, `confidence`, `provenance`). `KgDataSource` at `types.ts:54` is the lazy seam: `getNodes(ids?)` for seeds and `getNeighbors(id, { limit, cursor, direction, signal })` at `types.ts:58` for paged hops. `KgNeighborhood` at `types.ts:68` carries `facts`, `neighbors`, `nextCursor`/`hasMore`, and an optional `entity` — absence means "unknown id" and must fail, not fabricate (see §1.3).

`LabelMap` at `types.ts:12` is `Record<languageTag, string>` with `''` as fallback; `pickLabel` at `types.ts:87` prefers the requested language, then `''`, then `en/zh/…`, then an arbitrary key. `KgGraphData` at `types.ts:43` is the in-memory snapshot after adapter materialization.

`MemoryDataSource` at `packages/knowledge-graph/src/MemoryDataSource.ts:15` is the test/small-graph adapter: indexes facts by both endpoints (`out`/`inn` at `MemoryDataSource.ts:17`) so `getNeighbors` is `O(degree)`, dedups self-loops in `'both'` (`MemoryDataSource.ts:71`), and versions cursors as `"<version>:<offset>"` (`MemoryDataSource.ts:108`) — `load()` at `MemoryDataSource.ts:26` bumps `version` so in-flight cursors fail loudly instead of slicing a mutated list (`MemoryDataSource.ts:125`).

`rdf.ts:11` provides `parseRdfTurtle(text)` via `n3` `Parser`: subjects become entities, `rdf:type` sets `type` (last wins, `rdf.ts:51`), `rdfs:label`/`skos:prefLabel`/`schema:name` fill `labels` (`rdf.ts:56`), other object-IRI triples become `KgFact`s (`rdf.ts:62`), and every entity gets an `''` fallback (`rdf.ts:74`). Sync `Parser.parse` — not for multi-hundred-MB on the main thread (`rdf.ts:24` doc).

### 1.2 FixedZLayout — 2D projection without a fork

`FixedZLayout` at `packages/knowledge-graph/src/FixedZLayout.ts:22` wraps `VectoForceLayout` and clamps every `z` to a constant after each `step()` (`FixedZLayout.ts:49`) and after `setGraph` (`FixedZLayout.ts:37`). The inner simulation still runs as a 3D Barnes-Hut octree; `pinNode` delegates with `z ?? this.z` (`FixedZLayout.ts:56`), `sanitize()` at `FixedZLayout.ts:85` reseeds non-finite `x/y` onto a `cbrt` spiral. Pin contract diverges from `ForceLayout2D` (ID-addressed): `FixedZLayout` pins by index like `GraphLayout` (`GraphLayout.ts:46`), noted at `FixedZLayout.ts:18`.

### 1.3 KnowledgeGraphModel — the single layout driver

`KnowledgeGraphModel` at `packages/knowledge-graph/src/KnowledgeGraphModel.ts:62` is renderer-neutral and owns the paginated cut: `entities`/`facts`/`factKeys`/`expansions`/`requests`/`entityOrder`/`lastPositions` (`KnowledgeGraphModel.ts:69`). It is the **single driver** of its borrowed `GraphLayout` (`KnowledgeGraphModel.ts:43` doc: one `setGraph` per `rebuildGraph`, one `reheat` per `expand`). Construction takes `source`, optional borrowed `layout`, `pageSize`, `direction`, `lang` (`KnowledgeGraphModel.ts:39`).

- `bootstrap(focusIds, expandSeeds)` at `KnowledgeGraphModel.ts:114` fetches seeds via `getNodes`, `ingestEntities`, `rebuildGraph()`, then `expand` each seed.
- `expand(id)` at `KnowledgeGraphModel.ts:127` shares a promise per id (`KnowledgeGraphModel.ts:134`), short-circuits on `complete` (`KnowledgeGraphModel.ts:136`), marks `loading` (`KnowledgeGraphModel.ts:144`), then `loadPage` (`KnowledgeGraphModel.ts:240`).
- `loadPage` pages via `source.getNeighbors` with `AbortSignal` (`KnowledgeGraphModel.ts:246`), fails loudly if `page.entity` is missing (`KnowledgeGraphModel.ts:259` — never ingest a placeholder `'Unknown'` node), ingests entities/facts, advances `loaded` by **batch** `page.facts.length` not net-new (`KnowledgeGraphModel.ts:273`), records `ExpansionState` (`KnowledgeGraphModel.ts:275`), `rebuildGraph()` + `layout?.reheat(0.5)` (`KnowledgeGraphModel.ts:285`).
- `cancelExpand` at `KnowledgeGraphModel.ts:150` aborts via `AbortController`, marks `cancelled`.
- `rebuildGraph()` at `KnowledgeGraphModel.ts:332` captures layout positions, merges in stable `entityOrder`, seeds new nodes from `lastPositions`, builds `GraphData` with `pickLabel`, and calls `layout?.setGraph`.
- `dispose()` at `KnowledgeGraphModel.ts:225` deliberately does **not** dispose the borrowed layout — the session may still share it (`KnowledgeGraphModel.ts:230` comment).

Snapshot contract at `KnowledgeGraphModel.ts:23` is `{ version:1, entities, facts, expansions }`; `exportSnapshot`/`importSnapshot` preserve positions via `lastPositions` and revision-bump to abort stale requests (`KnowledgeGraphModel.ts:190`).

### 1.4 Graph3D + GraphLayout — the rendering seam

`GraphLayout` at `packages/graph3d/src/layout/GraphLayout.ts:12` is a minimal worker-friendly contract: `setGraph(data)`, `step(iterations) -> boolean` active/hot (`GraphLayout.ts:28` doc), flat `Float32Array positions` of xyz triplets in `GraphData` node order (`GraphLayout.ts:35`), optional `pinNode`/`unpinNode`/`reheat` by index. `Graph3D` at `packages/graph3d/src/Graph3D.ts:28` is two draw calls regardless of size: one `InstancedMesh` (`Graph3D.ts:115`) with `∛val` radius scaling (`Graph3D.ts:104`), one `LineSegments` (`Graph3D.ts:136`). `setGraphData` validates endpoints before attaching (`Graph3D.ts:73`) and `applyPositions` at `Graph3D.ts:149` writes matrices, tracks bounds inline (avoiding `computeBoundingSphere` cost — `Graph3D.ts:178` measured 60–78%), and warns once on short arrays (`Graph3D.ts:162`).

### 1.5 KnowledgeGraphSession — wiring

`KnowledgeGraphSession` at `packages/knowledge-graph/src/KnowledgeGraphSession.ts:67` owns `model`, `graph: Graph3D`, `camera: GraphCamera`, `layout: GraphLayout`, and `interaction: GraphInteraction`. Constructor at `KnowledgeGraphSession.ts:92` builds `Graph3D`, `GraphCamera` (mode is session `mode`), and a `FixedZLayout` (`z:0, repulsion 120, linkDistance 55` at `KnowledgeGraphSession.ts:109`) for `'2d'` else `VectoForceLayout`. The model is constructed with borrowed layout + `lang` (`KnowledgeGraphSession.ts:120`). Interaction wires `camera: () => camera.camera` so mode swaps stay live (`KnowledgeGraphSession.ts:129`), with `handleSelect`/`handleHover` routing to index-aligned `entityByIndex` (`KnowledgeGraphSession.ts:87`).

- `bootstrap` at `KnowledgeGraphSession.ts:182` awaits `model.bootstrap`, bails if disposed mid-flight (`KnowledgeGraphSession.ts:189`), then `syncFromModel()` + `camera.fitToPositions`.
- `syncFromModel()` at `KnowledgeGraphSession.ts:287` mirrors `model.getGraphData()` into `graph.setGraphData`/`applyPositions` and `interaction.setNodeCount`.
- `tick(iterations)` at `KnowledgeGraphSession.ts:242` steps layout, captures layout positions only when settled (`KnowledgeGraphSession.ts:250` — hot frames are not cached), applies to renderer. `tick` returns settled (`!stillHot`) matching `if (!tick()) rAF`.
- `expand` at `KnowledgeGraphSession.ts:219` delegates to model, mirrors, fires `onExpand`.
- `expandInBackground` at `KnowledgeGraphSession.ts:332` dedups in-flight expands per id (`inFlightExpansions` at `KnowledgeGraphSession.ts:85`), routes failures to `onError` or `console.error` — never unhandled (`KnowledgeGraphSession.ts:338`).
- `dispose()` at `KnowledgeGraphSession.ts:267` disposes interaction/camera/graph/layout/model in order; session owns the layout, model does not.

`GraphInteraction` at `packages/graph3d/src/GraphInteraction.ts:83` turns pointer events into `onHover`/`onSelect`/`onDrag` over `Graph3D` via `THREE.Raycaster` (`GraphInteraction.ts:168` `raycaster.setFromCamera`), building a normal-plane drag at `GraphInteraction.ts:300` and writing through `layout.pinNode` (`GraphInteraction.ts:309`). `setControlsEnabled(false)` at `GraphInteraction.ts:214` blocks host `OrbitControls` during drag.

Accessibility: `knowledge-graph` does **not** project per-node DOM (`KnowledgeGraphSession.ts:64` doc) — pair with an aggregate `role="status"` announcer in the host.

### 1.6 Frame integration and lifecycle

The session never owns the `WebGLRenderer` or the `requestAnimationFrame` loop — the host does (`KnowledgeGraphSession.ts:60` doc). Correct wiring is `attach(scene)` once (`KnowledgeGraphSession.ts:153`), `bootstrap` awaited (`KnowledgeGraphSession.ts:182`), then per-frame `tick()` + `render(renderer, scene)` (`KnowledgeGraphSession.ts:242`/`KnowledgeGraphSession.ts:256`). `tick` captures only settled positions (`KnowledgeGraphSession.ts:250` — hot-frame captures would write one `Map` entry per node per frame) and returns `true` when cooled (`KnowledgeGraphSession.ts:252`), so the loop is `if (!session.tick()) requestAnimationFrame(loop)` (`KnowledgeGraphSession.ts:240` doc). Post-await disposal guards at `KnowledgeGraphSession.ts:189`/`KnowledgeGraphSession.ts:227` quiesce late continuations from the constructor's fire-and-forget `bootstrap` (`KnowledgeGraphSession.ts:145`) — without them the mirror runs against a torn-down `Graph3D`.

`loadSnapshot` at `KnowledgeGraphSession.ts:202` is the demo/offline path: it builds a `KnowledgeGraphSnapshot` with every entity marked `complete` and `loaded = facts.length` (`KnowledgeGraphSession.ts:208`) so `expandOnSelect` (`KnowledgeGraphSession.ts:94` option, `KnowledgeGraphSession.ts:318` check) does not re-fetch hops already present. `setSize` at `KnowledgeGraphSession.ts:263` forwards to `camera.setSize`; `getMode` at `KnowledgeGraphSession.ts:160` exposes the session's `mode` (`'2d'|'3d'` at `types.ts:6`).

### 1.7 Graph data types and host ownership

`GraphData` at `packages/graph3d/src/types.ts:1` is `{ nodes: GraphNode[], links: GraphLink[] }` where `GraphNode` carries `id: NodeId` (`types.ts:6` `string|number`), optional `x/y/z` seeds, optional `fx/fy/fz` pins, `val` for `Graph3D` radius, and `color`. `GraphData` is the only object that crosses the model→layout→renderer seam — `KnowledgeGraphModel` builds it from `KgEntity`/`KgFact` (`KnowledgeGraphModel.ts:332` `pickLabel` + `position` spread), `GraphLayout.setGraph` consumes it by value (implementations clone or copy into SoA), and `Graph3D.setGraphData` indexes it by id (`Graph3D.ts:80` `indexById`). The renderer is deliberately ignorant of how positions were computed (`Graph3D.ts:26` doc: swappable or remote behind a worker), and the layout is agnostic of how the renderer batches (`VectoForceLayout.ts:68` doc). Host ownership is explicit: the caller constructs `VectoForceLayout`/`D3ForceLayout`/`FixedZLayout` and `Graph3D`/`GraphCamera`, the model borrows the layout (`KnowledgeGraphModel.ts:47` `layout?: GraphLayout` doc), and the session owns it (`KnowledgeGraphSession.ts:277` disposes). `D3ForceLayout` clones into `simNodes` (`D3ForceLayout.ts:71`) because d3 mutates its inputs; `VectoForceLayout` keeps f32 SoA (`VectoForceLayout.ts:88` `positions/vx/vy/vz/fx/fy/fz`) and never mutates caller nodes. `Graph3D.applyPositions` short-circuits on `positions.length < count*3` with a once-per-`setGraphData` warning (`Graph3D.ts:162` `hasWarnedShortPositions`, reset at `Graph3D.ts:100`) rather than writing NaN instance matrices that make the whole mesh frustum-culled.

## 2. Node Editor — document, commands, selection

### 2.1 Document model

`NodeDocument` at `packages/node-editor/src/model.ts:54` is `{ nodes: NodeData[], links: LinkData[] }` with immutable transforms (`cloneDocument` at `model.ts:78`, `deepCloneValue` at `model.ts:64` for `data` maps, `updateNodePosition` at `model.ts:93`). `NodeData` carries `position`, optional `width`/`height`, `ports: PortDefinition[]` (`model.ts:8` with `direction`, `dataType`, `maxConnections`), `data`; `LinkData` carries `source`/`target` + port ids (`model.ts:27`).

`validateLink` at `model.ts:126` checks source/target presence, `same-node` self-loop reject, `duplicate-link-id`, port presence + direction (`output`→`input`), `incompatible-types` on `dataType`, `duplicate-link` on directed endpoint pair, and `target-port-occupied` via `maxConnections` (`model.ts:152`). Cycles spanning multiple nodes are **allowed** — the doc is a user-authored flow (`model.ts:117` doc). `addLink`/`removeLink`/`removeNode` at `model.ts:163` enforce it; `removeNode` at `model.ts:178` removes incident links to stay referentially valid.

### 2.2 History and selection

`CommandHistory` at `packages/node-editor/src/history.ts:9` is a textbook undo/redo: `execute(label, after)` snapshots `before`/`after` via `cloneDocument`, pushes onto `undoStack`, clears `redoStack` (`history.ts:28`), with `undo()`/`redo()` swapping `current` (`history.ts:40`). All `NodeEditor` mutations go through this single gate so every user action is one history entry.

`SelectionState` at `packages/node-editor/src/selection.ts:9` tracks `ids: Set<string>` with `select(id, additive)` (`selection.ts:22`), `clear()`, `list()` snapshot, and a transient `drag: DragState | null` (`selection.ts:11`) for the current drag. `layoutDocument` at `packages/node-editor/src/layout.ts:14` is a deterministic layered layout: Tarjan SCC (`layout.ts:96` `stronglyConnectedComponents`) over the component DAG, Kahn topological rank assignment (`layout.ts:54`), then `x = originX + rank*horizontalGap`, `y = originY + index*verticalGap` (`layout.ts:85`). Deterministic by sorting ids and targets (`layout.ts:18`/`layout.ts:27`).

### 2.3 NodeEditor entity composition

`NodeEditor` at `packages/node-editor/src/editor.ts:199` extends `Entity` (`width 1000, height 700` defaults at `editor.ts:214`), owns `selection`, `history: CommandHistory` (`editor.ts:201`), `nodeEntities`/`linkEntities` maps (`editor.ts:202`), `status: StatusAnnouncer` (`editor.ts:204`), and transient `dragDocument`/`connection`/`connectionPoint` (`editor.ts:205`). Children are `NodeCard` (`editor.ts:143`), `LinkEntity` (`editor.ts:119`), `PortEntity` (`editor.ts:51`), and `StatusAnnouncer` (`editor.ts:29`).

- `NodeCard` at `editor.ts:143` is a `UIComponent` with card chrome, owns `PortEntity` children (`editor.ts:162`), advertises `role="button"` with roving `tabIndex` via `selection.has` (`editor.ts:174`), and routes `pointerdown/move/up` to `editor.beginDrag/moveDrag/endDrag` (`editor.ts:155`).
- `PortEntity` at `editor.ts:51` is a 12px circle (`editor.ts:20`), output ports right-aligned (`editor.ts:62`), routes pointerdown into `editor.beginConnection` (`editor.ts:66`) and `click` with `KeyboardEvent` provenance into `editor.portActivated` (`editor.ts:90` — filters out browser-synthesized clicks from pointer capture).
- `LinkEntity` at `editor.ts:119` paints source→target lines reading live `nodeEntities` positions (`editor.ts:136`).
- `StatusAnnouncer` at `editor.ts:29` is an invisible `role="status"` live region (`aria-live polite`, `editor.ts:40`) for keyboard-only connection state (no rubber line to see).

Editing operations:

- Drag: `beginDrag` at `editor.ts:406` snapshots `dragDocument` via `cloneDocument`, sets `selection.drag` with origins. `moveDrag` at `editor.ts:421` is the hot path: **mutates** `node.position` in place and `setPosition` on the card, no per-move clone — `LinkEntity` reads the same `nodeEntities` map so links follow with no rebuild (`editor.ts:427` comment). `endDrag` at `editor.ts:441` compares JSON before/after and pushes one `'Move node'` entry; `cancelDrag` at `editor.ts:452` rolls back via `applyPreview` from `dragDocument`.
- Connection: `beginConnection`/`moveConnection`/`endConnection` at `editor.ts:282` handle rubber line; `portActivated` at `editor.ts:297` is the WCAG 2.1.1 parity — output port arms a pending connection (no rubber point, announces via `status.say` at `editor.ts:307`), input port commits via `commitLink` (`editor.ts:334`). `commitLink` builds `link:id = link:<src>:<port>:<tgt>:<port>` (`editor.ts:337`) and calls `createLink` (`editor.ts:380` pushes `addLink` through history). `handleKeyDown` at `editor.ts:459` maps `Escape` to cancel both gestures, `Ctrl/Cmd+Z/Y` to undo/redo (cancelling transient gestures first at `editor.ts:474`), and `Delete/Backspace` to `deleteNodes(selection.list())` (`editor.ts:480`).
- Auto-layout: `applyAutoLayout` at `editor.ts:274` runs `layoutDocument`, pushes one `'Auto-layout'` entry when changed (`editor.ts:277`).

Persistence at `packages/node-editor/src/persistence.ts:168` is `exportDocument(document) -> string` (validates first at `persistence.ts:169`, wraps with `schemaVersion: 1` at `persistence.ts:171`, `cloneJson` via `JSON.parse(JSON.stringify(...))` at `persistence.ts:164`) and `importDocument(serialized)` at `persistence.ts:178` (parses, checks `schemaVersion` at `persistence.ts:186`, then `validateDocument` at `persistence.ts:84` — structural checks plus a `validateLink`-against-peers semantic pass at `persistence.ts:156` that strips each link from the set before validating, so an at-capacity legitimate link does not falsely fail its own occupancy test). Rejects non-finite positions, duplicate ids/ports, missing port refs, wrong `direction`, and `dataType` mismatches (`persistence.ts:70`). `NodeEditorPersistenceError` at `persistence.ts:24` is the failure contract. `isJsonValue` at `persistence.ts:41` guards `data` maps for JSON-safe cycles (`WeakSet` at `persistence.ts:45`), non-finite numbers (`persistence.ts:43`), and symbol keys (`persistence.ts:50`), so a persisted `NodeDocument` round-trips through `JSON.stringify` losslessly — any `Map`/`Set`/`Date` in `data` must be serialized to plain JSON before export.

### 2.4 Known traps in this vertical

Editor-specific pitfalls that have cost real review time (see `packages/node-editor/src/editor.ts` comments inline):

- **Pointer provenance filter** — `PortEntity` click handler at `editor.ts:90` gates on `nativeEvent instanceof KeyboardEvent` because core also synthesizes `click` for native browser clicks on the same mirror (pointer capture retargets a released connect-drag back onto it). Without the filter, a released connect would both complete and re-arm a pending keyboard connection.
- **Drag aliasing** — `moveDrag` mutates `documentState`'s own node object (`editor.ts:427` "owns its node objects via `cloneDocument`") so the in-place edit cannot alias a history snapshot; `dragDocument` (the pre-drag clone at `editor.ts:411`) stays clean for `cancelDrag`.
- **History contamination** — `endDrag` only pushes when the JSON changed (`editor.ts:445` `JSON.stringify(before) !== JSON.stringify(current)`), and `applyAutoLayout` the same (`editor.ts:276`). A no-op drag otherwise adds an undo entry that does nothing but shift the redo stack.
- **Transient gesture scope** — `deleteNodes` at `editor.ts:390` and the `Ctrl+Z/Y` path at `editor.ts:474` both cancel pending `connection`/`drag` before mutating history, so the history command sees the true document, not a half-moved node.

## 3. Desktop Shell — a window manager on Scene

### 3.1 Shell and layout

`DesktopShell` at `packages/desktop/src/DesktopShell.ts:87` is the top-level host (wallpaper + `DisplayLayout` + `WindowManager` + `Taskbar`/`StartMenu` + `ShortcutRouter`). Constructor at `DesktopShell.ts:105` resolves config (`resolveConfig`), registers `AppRegistry` (`DesktopShell.ts:108`), constructs `DisplayLayout` (`DesktopShell.ts:116`), `WindowManager` (`DesktopShell.ts:124`), `Wallpaper` (`DesktopShell.ts:133`), and `ShortcutRouter` (`DesktopShell.ts:140`). `Wallpaper` at `DesktopShell.ts:17` is a non-interactive cover image with `a11yProjection never` (`DesktopShell.ts:26`).

`start()` at `DesktopShell.ts:152` is idempotent: adds wallpaper, syncs layout, mounts taskbar, attaches shortcuts and document-level `pointerdown`/`keydown` for Kickoff dismissal and Escape (`DesktopShell.ts:143`). `syncLayoutToScene`/`resize` at `DesktopShell.ts:171` reads live `scene.width/height` (no resize bus) and updates wallpaper + taskbar placement. `setTheme` at `DesktopShell.ts:204` swaps tokens, `WindowChrome`, and remounts taskbar.

`DisplayLayout` at `packages/desktop/src/DisplayLayout.ts:15` maps logical displays to a union and subtracts the taskbar strip to get `workArea` (`DisplayLayout.ts:50`), `bounds()` (`DisplayLayout.ts:60`) for wallpaper, `displayAt` point query (`DisplayLayout.ts:80`), `clampRect` for window placement (`DisplayLayout.ts:89`), and `updateSceneSize` for single-display reuse (`DisplayLayout.ts:105`).

### 3.2 WindowManager — open/focus/close/z-order

`WindowManager` at `packages/desktop/src/WindowManager.ts:65` owns `DesktopWindow[]` (`WindowManager.ts:71`), `focused` (`WindowManager.ts:72`), `cascade`/`seq` (`WindowManager.ts:73`), and dialog stacks `dialogOrder`/`dialogPrevFocus` (`WindowManager.ts:77`). Methods:

- `open(appId, opts)` at `WindowManager.ts:126` enforces `instances: 'single'|'multiple'` (`WindowManager.ts:132` — focus existing unless `forceNew`), cascades `(cascade % 8)*28` (`WindowManager.ts:144`), clamps via `layout.clampRect` (`WindowManager.ts:149`), assigns `windowId = appId-seq` (`WindowManager.ts:151`), creates `DesktopWindow` (`WindowManager.ts:152`), `scene.showOverlay` (`WindowManager.ts:170`), and `focus` (`WindowManager.ts:172`).
- `openDialog(opts)` at `WindowManager.ts:184` is registry-free: close-only chrome, excluded from taskbar (`Taskbar.ts:159`), modality-held focus (`WindowManager.ts:238`), centered by default (`WindowManager.ts:191`), with `onDocKeyDown` trapping `Escape` for the top `dismissible` dialog (`WindowManager.ts:329`).
- `focus` at `WindowManager.ts:233` blocks refocus when a modal dialog is topmost (`WindowManager.ts:237`), restores/unpins a11y via `requestA11yProjection`/`releaseA11yProjection` (`WindowManager.ts:248`/`WindowManager.ts:253`), and `restack` without `Entity.remove` (`WindowManager.ts:339` — splice `overlayRoot.children` + `markStructureChanged` to avoid thrashing drivers).
- `close` at `WindowManager.ts:258` splices, hides overlay, `win.destroy()`, restores `dialogPrevFocus` (`WindowManager.ts:281`), and picks the next visible window reverse-order (`WindowManager.ts:278`).
- `cycleFocus(backward)` at `WindowManager.ts:297` and `topModal()` at `WindowManager.ts:311`.

Listeners via `on(listener)` at `WindowManager.ts:116` emit `open/close/focus/state`.

### 3.3 DesktopWindow — chrome and interaction

`DesktopWindow` at `packages/desktop/src/Window.ts:158` is a `UIComponent` with `a11yProjection onDemand` (`Window.ts:228`) and `pointerEvents none` dialog mirror (`Window.ts:400`). Structure: `Card shell` + `Card titlebar` + `Text titleLabel` + `TitlebarDragHandle` + three `Button` chrome (`close/max/min` via `makeChromeBtn` at `Window.ts:361`) + `ResizeGrips` + `ClientHost` (`Window.ts:79` invisible clipping host) + `content` from `app.create(ctx)` (`Window.ts:340`). Chroming via `WindowChrome` (`Window.ts:8`) from tokens (`DesktopShell.ts:411` `resolveChrome`).

- Geometry: `applyGeom` at `Window.ts:507` clamps to `minWidth/minHeight` (`Window.ts:498` `max(chrome, app)`), sizes shell/titlebar/clientHost, repositions chrome buttons and drag handle (`Window.ts:524` `chromeBtnStripWidth`), and `layoutClientContent` stretches `content.width/height` (`Window.ts:538`).
- Titlebar drag: `TitlebarDragHandle` at `Window.ts:133` is a dedicated interactive `UIComponent` (`role button, label Move window`, `tabIndex 0`) with eager a11y so the hit target exists without pinning the whole dialog (`Window.ts:282`). `beginTitlebarDrag` at `Window.ts:589` handles restore-on-drag when maximized (`Window.ts:594`), `handleMoveKey` at `Window.ts:670` maps `ArrowLeft/Right/Up/Down` with `Shift=1px` (`Window.ts:671`), `clampMovePosition` keeps titlebar in work area (`Window.ts:700`).
- Resize: `hitResizeEdge` at `Window.ts:553` with 6px rim, `handleResizePointerDown` at `Window.ts:615` (children own their hits, client host is `isPointInside false`, so only rim lands here), `applyResize` at `Window.ts:707` per-edge with min clamps and work-area clipping, doc-level capture via `attachDocPointers` at `Window.ts:638`.
- State: `maximize`/`restore`/`toggleMaximize` at `Window.ts:433`, `minimize`/`restoreFromMinimized` at `Window.ts:468` (opacity 0 + `interactive false` + `a11yHidden true`), `setGeometry` at `Window.ts:488`.
- Focus ring: `setFocused` at `Window.ts:404` swaps `shell.border` to `focusRing`; `updateChrome` at `Window.ts:411` merges.

`Taskbar` at `packages/desktop/src/Taskbar.ts:36` is a Plasma-style bar: `Card bar` + 54px `Start` button (`Taskbar.ts:73`), `Text clockLabel` (`Taskbar.ts:94`, throttled to minute ticks at `Taskbar.ts:127`), `EntriesHost` (`Taskbar.ts:103` clipped host), `entryButtons: Map<DesktopWindow, Button>` (`Taskbar.ts:47`), `wm.on(() => rebuild)` (`Taskbar.ts:120`). `rebuild()` at `Taskbar.ts:157` filters dialogs (`Taskbar.ts:159`), pools buttons keyed by window (`Taskbar.ts:171`), eager a11y with live `selected` binding (`Taskbar.ts:194`), caps at `EntriesHost.width` (`Taskbar.ts:207`), and click-on-active minimizes (`Taskbar.ts:220`).

`StartMenu` at `packages/desktop/src/StartMenu.ts:42` (Kickoff) is a `240px` `Card` panel (`StartMenu.ts:67`) with one `Button` per app (`StartMenu.ts:92`), `startMenuHeight` helper at `StartMenu.ts:31` shared with `DesktopShell` for pre-positioning (so shelf + menu cannot drift, `DesktopShell.ts:303`). Shell shows it via `scene.showOverlay` + `requestA11yProjection` (`DesktopShell.ts:330`) and bounds it by taskbar position (`DesktopShell.ts:305`).

## 4. Table — virtualized grid over Text

### 4.1 Construction contracts

`Table` at `packages/table/src/Table.ts:144` extends `UIComponent`. Construction normalizes every input: `normalizeColumnWidths` (`Table.ts:789`) scales to `width` (equal-split when missing/invalid), `normalizeColumnAlign` (`Table.ts:779`) defaults to `left`, `normalizeCell`/`normalizeRow` (`Table.ts:810`) pad short rows with `''`, truncate long ones (`Table.ts:806` doc), create one `Text` per string via `createTextCell` (`Table.ts:921` — `bold` for headers, `selectable` from option), and reject duplicate `Entity` cells via `seenCells` (`Table.ts:228`). `CELL_PADDING_PX = 12` at `Table.ts:142` is shared by `fitCell` wrap width (`Table.ts:935` `maxWidth = colWidths[col] - 2*CELL_PADDING`) and `cellX` alignment inset (`Table.ts:767`).

Virtualization opt-in: `viewportHeight` at `Table.ts:50`. When `virtualized` (`Table.ts:177` `viewportHeight>0`), body cells are `null` until window materialization (`Table.ts:256` `bodyCells = rows.map(() => null)`, `reserveRowEntities` at `Table.ts:822` validates `Entity` cells eagerly), a `TableBodyClip` entity (`Table.ts:261` `clipChildren true`) owns the body, header stays pinned direct, and `bindScroll` at `Table.ts:282` installs `wheel` + `pointerdown/move` drag-to-scroll.

### 4.2 Layout and virtualization

`layout()` at `Table.ts:987` recomputes `headerHeight` from fitted `headerCells`, positions them via `cellX` (`Table.ts:999`), then branches:

- Virtualized: fixed `rowHeights = baseRowHeight` per row (`Table.ts:1009`) so `scroll↔row-index` is `O(1)` (`Table.ts:1004` comment — walking every row would be `O(rows)` and defeat windowing), sizes `bodyClip` to `viewportHeight - headerHeight` (`Table.ts:1013`), and defers body cell sync to `reconcileVirtualRows` (`Table.ts:392`) which mounts exactly `[first, last]` rows where `first = floor(scrollY/rh) - overscan` (`Table.ts:397`) and `last = ceil((scrollY+bodyViewport)/rh) + overscan -1` (`Table.ts:404`) — one less than the old `+overscan` counted an invisible extra row.
- Classic: measures `rowHeights` per row (`Table.ts:1042`), builds `rowTops` prefix sums (`Table.ts:1053` `rebuildRowTops`), and positions every row (`Table.ts:1056`).

Scroll integration at `Table.ts:352` `update(dt)` is a dt-aware exponential integrator (mirrors `VirtualList`/`Tree`): `velY += diff*7.2*(dt/1000)` + `velY *= exp(-dt/84)` (`Table.ts:361`), threshold `0.05` (`Table.ts:363`), with `hasPendingAnimations` at `Table.ts:378` so `onDemand` never idles mid-scroll (`Table.ts:378` doc).

`appendRows` at `Table.ts:885` is append-only by design: pushes to `rows` + `bodyCells` (reserving `Entity` cells in virtualized mode at `Table.ts:895`), then `layout()` reflows and `_syncGridA11y` re-pools — no `detachA11y` or tab-stop invalidation on growth (`Table.ts:870` doc).

### 4.3 Grid a11y — pooled row/cell hotspots

`RowHotspot` at `Table.ts:55` is a transparent structural `role="row"` container (`pointerEvents none`, `Table.ts:70`, `layoutControlledProperties` at `Table.ts:65`). `GridCellHotspot` at `Table.ts:82` is a focusable `role gridcell|columnheader` (`Table.ts:111`) with roving `tabIndex` via `isGridTabStop` (`Table.ts:473`), `pointerEvents none` so selectable text underneath owns the pointer (`Table.ts:119`), and `keydown` forwarding to `handleGridKey` (`Table.ts:91`).

`_syncGridA11y()` at `Table.ts:624` projects the ARIA grid: one pinned `headerRow` with `columnheader` hotspots per column (`Table.ts:630`), plus a pooled `bodyRowPool: { row, cells }[]` (`Table.ts:199`) grown/shrunk to `need = last-first+1` (`Table.ts:691`), each with `GridCellHotspot` per column re-bound via `bind(rowIndex,colIndex,label)` (`Table.ts:95`). Pool entries re-parent between `Table` and `bodyClip` when mode changes (`Table.ts:713`). The re-anchor protocol at `Table.ts:412`/`Table.ts:668` preserves the roving stop when the focused row scrolls out: `pendingFocusReanchor` + `reanchorRestoreFocus` via `activeCellHoldsFocus` (`Table.ts:592` checks `document.activeElement` vs `scene.getA11yElement`) so focus is only restored when the unmounted cell genuinely held DOM focus — never stealing it.

Keyboard model: `handleGridKey` at `Table.ts:490` (Arrow, `Home`/`End`, `Ctrl+Home/End` to edges, `PageUp/PageDown` by `pageRows()` at `Table.ts:548`), `_focusCell` at `Table.ts:560` scrolls the row into view (`_scrollRowIntoView` at `Table.ts:603`) and `focus()`es the hotspot, with re-anchor before `tabIndex` is bound.

## 5. How verticals compose primitives

| Vertical            | Primitive stack                                                                                                                    | Key file:line boundaries                                                                                                                                      |
| ------------------- | ---------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Knowledge Graph** | `@vectojs/graph3d` layout contract + Barnes-Hut 3D/2D + `Graph3D`/`GraphCamera`/`GraphInteraction`                                 | `KnowledgeGraphModel.ts:62` model, `FixedZLayout.ts:22` adapter, `KnowledgeGraphSession.ts:67` wiring, `Graph3D.ts:28` renderer, `GraphLayout.ts:12` contract |
| **Node Editor**     | `CommandHistory` snapshots + `SelectionState` + `layoutDocument` SCC + `Entity`/`UIComponent` tree + `StatusAnnouncer` live region | `model.ts:54` doc, `history.ts:9` history, `selection.ts:9` selection, `layout.ts:14` auto-layout, `editor.ts:199` editor, `editor.ts:51` ports               |
| **Desktop**         | `Scene` overlay + `DisplayLayout` work areas + `WindowManager` z-order + `DesktopWindow` chrome + `Taskbar`/`StartMenu`            | `DesktopShell.ts:87` shell, `DisplayLayout.ts:15` display, `WindowManager.ts:65` WM, `Window.ts:158` window, `Taskbar.ts:36` taskbar, `StartMenu.ts:42` menu  |
| **Table**           | `Text` cells + `RowHotspot`/`GridCellHotspot` a11y pool + `TableBodyClip` + exponential scroll integrator                          | `Table.ts:144` table, `Table.ts:55` row, `Table.ts:82` cell, `Table.ts:392` reconcile, `Table.ts:624` a11y sync, `Table.ts:352` scroll                        |

Each vertical borrows its primitive and disposes only what it constructed (model-borrowed layout at `KnowledgeGraphModel.ts:230`, session-owned layout at `KnowledgeGraphSession.ts:277`; editor owns history/selection; shell owns WM/layout/taskbar/wallpaper).

## 5a. Desktop deep cut — registry, VFS, shortcuts, and chrome

`AppRegistry` at `packages/desktop/src/AppRegistry.ts:1` is a thin `Map<id, AppDefinition>` over `ResolvedWebosConfig.apps` with `get(id)`, `list()`, and `has(id)` — the WM's `open` gate at `WindowManager.ts:128` throws `unknown app id` when it misses. `AppDefinition` at `packages/desktop/src/types.ts:1` carries `id`, `title`, `icon`/`iconSvg`, `create(ctx: AppContext) -> Entity`, `instances: 'single'|'multiple'`, `defaultWidth/Height`, `minWidth/Height`, and category metadata used by `StartMenu`.

`Vfs` at `packages/desktop/src/Vfs.ts:1` is the optional in-memory file surface passed through `AppContext.vfs` into every `create()` call — apps that need file pickers or drive views bind to it, and `WindowManager` threads the same instance into every `DesktopWindow` (`WindowManager.ts:86`). `resolveConfig` at `packages/desktop/src/resolveConfig.ts:1` merges user `WebosConfig` into `ResolvedWebosConfig` (desktop, displays, theme, apps, shortcuts, vfs) with defaults for `taskbarHeight`/`taskbarPosition`/`wallpaper` (`resolveConfig.ts:12`).

`ShortcutRouter` at `packages/desktop/src/ShortcutRouter.ts:1` maps `chord -> ShortcutAction` (`types.ts:1` `open-app | close-focused | toggle-start | custom`) via a normalized `Map`, with `attach()`/`detach()` at `ShortcutRouter.ts:30` binding the single `keydown` listener that `DesktopShell` enables in `start()` (`DesktopShell.ts:158` `shortcuts.attach()`). `dispatchShortcut` at `DesktopShell.ts:344` switches on `action.type` — `open-app` calls `windowManager.open(action.appId)` (`DesktopShell.ts:347`), `close-focused` calls `closeFocused()` (`DesktopShell.ts:350`), `toggle-start` flips Kickoff (`DesktopShell.ts:353`), `custom` forwards to `onCustomShortcut` (`DesktopShell.ts:356`).

`icon.ts:1` keeps `WINDOW_ICONS` SVG strings (`close/maximize/minimize`) and `addButtonIcon(button, svg, size, color)` (`icon.ts:12`) which injects the icon into a `Button`'s label — used by `Window.makeChromeBtn` at `Window.ts:380` and `Taskbar.rebuild` at `Taskbar.ts:185` for per-window app icons scaled to `16` with `chrome.fg`.

`DesktopShell` chrome resolution at `DesktopShell.ts:411` `resolveChrome` maps theme tokens to `WindowChrome` (`Window.ts:8`) with `str()`/`num()` helpers (`DesktopShell.ts:401`) — `windowBg`, `windowBorder`, `titlebarBg/Fg`, `titlebarHeight`, `closeBg/Fg`, `focusRing`, `radius`, `resizeHandle`, `minWidth/Height` — so a token rename cannot silently fall back to `undefined` (both sites pass full `resolveChrome` objects, noted at `Window.ts:414`). `Taskbar` chrome at `Taskbar.ts:7` `TaskbarChrome` and `StartMenu` chrome at `StartMenu.ts:6` `StartMenuChrome` follow the same token-threading.

## 5a. Desktop deep cut — continued: taskbar lifecycle and start menu ownership

`Taskbar` at `Taskbar.ts:36` pools per-window entry buttons in `Map<DesktopWindow, Button>` (`Taskbar.ts:47`) and rebuilds on every `wm.on('open'|'close'|'focus'|'state')` (`WindowManager.ts:116` → `Taskbar.ts:120`). Dialog windows are explicitly filtered (`Taskbar.ts:159` `!w.isDialog`), so `openDialog` never appears as a task entry. Each entry caps at `maxW 160` (`Taskbar.ts:162`), carries the app icon via `addButtonIcon` (`Taskbar.ts:185` `16`px, `chrome.fg`), and lives under `EntriesHost` (`Taskbar.ts:103` `clipChildren true`) so overflow clips before the clock label (`Taskbar.ts:106` comment). Clicking an active (focused, non-minimized) entry minimizes (`Taskbar.ts:220` `win.minimize()`), otherwise focuses (`Taskbar.ts:224` `wm.focus`).

`StartMenu` at `StartMenu.ts:42` is shown via `scene.showOverlay` + `requestA11yProjection` (`DesktopShell.ts:330`) and dismissed by `pointerdown` outside its box (`DesktopShell.ts:361` `handleOutsidePointer` using `scene.clientToScene` at `DesktopShell.ts:366`), by `keydown Escape` (`DesktopShell.ts:144` `onDocKeyDown`), or by clicking the Start button again (toggle). `startMenuHeight` at `StartMenu.ts:31` is the single source of truth for both the shell's pre-positioning (`DesktopShell.ts:303` `estH`) and the menu's own height (`StartMenu.ts:58`), so the two cannot drift. The shell's `toggleStartMenu` (`DesktopShell.ts:196`) and `closeStartMenu`/`openStartMenu` (`DesktopShell.ts:335`/`DesktopShell.ts:294`) are the only callers — verticals must not add a second menu owner without consolidating this pair.

## 5b. Table deep cut — width, entity cells, and chrome

`setWidth(width)` at `packages/table/src/Table.ts:963` rescales `colWidths` proportionally (`Table.ts:972` `colWidths.map(c => c/total*next)`) rather than re-splitting equally — a caller-supplied `colWidths` ratio survives a resize. `layout()` must then be called (chained at `Table.ts:975` via `return this.layout()`), because `colWidths` is the source for `fitCell` wrap widths (`Table.ts:935`), `cellX` alignment (`Table.ts:767`), and every child `x` (`Table.ts:999`/`Table.ts:1062`).

Entity cells (not strings) are accepted at `Table.ts:910` `normalizeCell` and `Table.ts:822` `reserveRowEntities` with `seenCells: Set<Entity>` at `Table.ts:228` to reject duplicate instances eagerly — even in virtualized mode where `Text` construction is deferred to `reconcileVirtualRows` (`Table.ts:392`), the `Entity` identity check runs at append time (`Table.ts:895`) so `Entity.add` never silently re-parents a cell out of its original slot. `setCellSelectable` at `Table.ts:930` and `fitCell` at `Table.ts:935` are the two per-cell capability probes (`SizableCell`/`SelectableCell` interfaces at `Table.ts:5`).

`getContentProjection() -> null` at `Table.ts:1097` declares that `Table` never duplicates child text — cell `Text` entities own their own projection, so the table's own a11y role (`getA11yAttributes` at `Table.ts:1088` `role grid, label "Data table with N cols and M rows", pointerEvents none`) is purely structural. Chrome paint at `Table.ts:1101` `render(r)` draws the `roundRect` fill (`Table.ts:1103`), header fill (`Table.ts:1107`), column dividers (`Table.ts:1114`), row separators (virtualized at `Table.ts:1124` over `[first,last)` viewport-relative, classic at `Table.ts:1139` over `rowHeights`), and outer border (`Table.ts:1150`).

`GraphCamera` at `packages/graph3d/src/GraphCamera.ts:1` wraps `THREE.PerspectiveCamera | OrthographicCamera` with `mode: '2d'|'3d'` switching, `domElement` binding, `setSize`/`fitToPositions` (`KnowledgeGraphSession.ts:191` calls it post-bootstrap), and `setEnabled` for the interaction's `setControlsEnabled` gate (`KnowledgeGraphSession.ts:132`). `Graph3D.pickNode` at `packages/graph3d/src/Graph3D.ts:246` is the `raycaster.intersectObject(nodeMesh)` path that `GraphInteraction.pick` at `GraphInteraction.ts:168` builds from NDC.

### 4.4 Choosing between VectoForceLayout and D3ForceLayout

The two `GraphLayout` implementations at `packages/graph3d/src/layout/` share `setGraph`/`step`/`positions`/`pinNode`/`reheat` but differ in feel: `VectoForceLayout` at `VectoForceLayout.ts:68` is an in-house Barnes-Hut octree with explicit `repulsion * alpha` + link springs + `centerStrength` + `velocityDecay` tick (`VectoForceLayout.ts:233` six phases); `D3ForceLayout` at `D3ForceLayout.ts:37` is a `d3-force-3d` adapter (`forceSimulation(…,3).force('link', forceLink).force('charge', forceManyBody).force('center', forceCenter)` at `D3ForceLayout.ts:88`, `chargeStrength -30` default at `D3ForceLayout.ts:16`). Pick `VectoForce` for seeded determinism and an invisible WASM accelerator (`VectoForceLayout.ts:108` `forceBackend` with `enableWasmForce` at `VectoForceLayout.ts:196` streaming vs `enableWasmForceSync` at `VectoForceLayout.ts:209` bytes), `D3Force` when migrating an existing `3d-force-graph` scene whose feel is already tuned — both are swappable behind `GraphLayout` so `KnowledgeGraphSession` can stay agnostic.

## 6. App traps vs engine defects — the forge split and the dogfooding loop

`vectojs-docs/forge/findings/app-level-and-toolchain.md:1` names the split explicitly: app-level findings are _not_ engine defects but are kept because diagnosis cost real effort. The `forge/findings/README.md` template plus the per-area index makes `app-level-and-toolchain` vs `core-*`/`simulation-*`/`text-*` unmistakable at review — ambiguous entries drift into engine triage and waste repair budget. Status is append-only (`app-level-and-toolchain.md:5`), with `Upstream status` updated when a fix ships (`app-level-and-toolchain.md:8`).

Representative app traps, each a vertical composition lesson:

- **Sandboxed iframe opaque origin** (`app-level-and-toolchain.md:30` 2026-07-16): a motif gallery `sandbox="allow-scripts"` without `allow-same-origin` gives an opaque origin; ES-module + `importmap` fetches become CORS and a same-origin `./demo.js` never loads — blank with no parent console error. Fix: `allow-scripts allow-same-origin` + real static-route `src` (`app-level-and-toolchain.md:51`), not `srcdoc` (resolves to `about:srcdoc`, `app-level-and-toolchain.md:48`). Lesson for verticals: trusted first-party embeds need a real origin; untrusted code needs a separate subdomain, not a silent iframe attribute change.
- **Canvas count ≠ booted** (`app-level-and-toolchain.md:66`): host polled `querySelectorAll('canvas').length >= 2` assuming a second WebGL point-layer canvas means "ready" — canvas2D demos and `ThreeAdapter` (offscreen `canvas`, not in DOM) never reach 2, so the "Loading…" overlay never hid. Fix: `canvas.width > 0` backing-store check (`app-level-and-toolchain.md:80`). Lesson: vertical readiness probes must key off backing-store size, not DOM canvas count; backend count is an implementation detail.
- **EPUB image pipeline** (`app-level-and-toolchain.md:94` 2026-07-19 trio): `body.textContent` silently drops `<img>`/SVG `<image>` (`app-level-and-toolchain.md:99`), so manga EPUBs render empty; interpolated `alt` without `escapeMarkdown` lets `foo](javascript:…)` become a clickable link via `marked` reparsing (`app-level-and-toolchain.md:155`); and `data:image/jpeg;base64,…` tokenized by `[a-zA-Z0-9]+` splits at every `+`/`/` into ~20k tokens, typing out base64 as gibberish for minutes (`app-level-and-toolchain.md:185`). Fixes: recursive `xhtmlNodeToMarkdown` with base64 `![alt](data:…)` + `kind:"markdown"` (`app-level-and-toolchain.md:103`), `escapeMarkdown` (`app-level-and-toolchain.md:167`), and `!\\[[^\\]]*\\]\\([^)]*\\)` as first tokenizer alternative for atomic image reveal (`app-level-and-toolchain.md:204`). Lesson: any extractor that will be reparsed must escape the target format, and typewriter tokenizers need a non-text atomic opt-out.
- **Lex coalescing + `hasPendingAnimations`** (`app-level-and-toolchain.md:222`): `MathMarkdown.appendMarkdown` fired `marked.lexer` per chunk (`O(n²)` over accumulated text, `app-level-and-toolchain.md:236`), and `StreamReader` lacked `hasPendingAnimations()`, so an `onDemand` scene idled mid-stream when `update()` added zero characters (`app-level-and-toolchain.md:246`). Fix: at-most-one lex in flight + `docEpoch` coalescing (`app-level-and-toolchain.md:254`) and `hasPendingAnimations() === status==="streaming"` (`app-level-and-toolchain.md:259`). Lesson: any entity that marks dirty from inside `update()` needs `hasPendingAnimations` or `onDemand` stalls — `Table` already has it at `Table.ts:378`.
- **CodeQL without config** (`app-level-and-toolchain.md:369` 2026-08-06): `.github/workflows/codeql.yml` passed `queries: security-and-quality` with no `config-file`, so vendored `packages/tex/src/kernel` lit 19 alerts (including one high-severity false positive in KaTeX `replace("*","")`, `app-level-and-toolchain.md:375`). Fix: `.github/codeql/codeql-config.yml` with `paths-ignore` for `kernel`/`glyphs` (`app-level-and-toolchain.md:384`).
- **Vendor-katex ordering** (`app-level-and-toolchain.md:337`): `checkHandWritten()` ran after `rmSync` that clears `src/kernel/`, so a throw left a missing `VENDORED.md`; `--check` always spuriously diffed 16 lines because it never wrote the manifest on that path (`app-level-and-toolchain.md:349`). Fix: validate before destructive step, write manifest before compare (`app-level-and-toolchain.md:354`).
- **`sideEffects: false` killed the tex registry** (`app-level-and-toolchain.md:404`): `packages/tex/package.json` `sideEffects: false` let esbuild drop bare `import './x'` that populates KaTeX registries — `layout('x')` threw `Got group of unknown type: 'mathord'` (`app-level-and-toolchain.md:407`). Suite stayed green because `vitest` aliases `@vectojs/*` to `src/` and never loads `dist/` (`app-level-and-toolchain.md:416`). Fix: `sideEffects: true` + manifest assertion in `glyphCodec.test.ts` (`app-level-and-toolchain.md:430`).
- **Bun cache race + wrangler fallback** (`app-level-and-toolchain.md:15` + `:125`): `bun install` served mixed-version `dist` after publish (`app-level-and-toolchain.md:17`) and `cloudflare/wrangler-action` fallback (`bun i wrangler@3.90.0`) failed tarball extraction when the repo had no `wrangler` dep (`app-level-and-toolchain.md:129`). Fixes: `rm -rf node_modules && bun install --force` (`app-level-and-toolchain.md:24`) and pin `wrangler` as `devDependency` (`app-level-and-toolchain.md:140`).
- **Repository hygiene** (`app-level-and-toolchain.md:276` 2026-08-06): committed `node_modules` blew the clone to 75 MB, held by 4 stale refs — no `main`-reachable blobs; fix was ref deletion + `git gc --prune=now` (`app-level-and-toolchain.md:299`), fresh clone `.git` 75M→6.8M (`app-level-and-toolchain.md:324`).
- **Publishing hygiene** (`app-level-and-toolchain.md:447` 2026-08-15): `packages/desktop/package.json` shipped `workspace:*` in `dependencies` (`app-level-and-toolchain.md:455`) because CI never probed `bun install` from the tarball. Fix: move to `peerDependencies` + `devDependencies workspace:*` matching `ui` (`app-level-and-toolchain.md:467`).
- **Benchmark runner resurrected tmp/** (`app-level-and-toolchain.md:518` 2026-08-24): `runBenchmarkSuite()` mkdir before try (`benchmarks/runner/runner.ts:323`) left `tmp/benchmark-runner` after every early failure; fix moved scratch to `os.tmpdir()` (`app-level-and-toolchain.md:530`).

The dogfooding loop closes the circle: `vectojs-native/AGENTS.md` declares `vectojs-native` a container of forge apps (not a repo), each documenting itself in a sibling `<app>-docs/` while only engine findings upstream to `vectojs-docs/forge/findings/` — exactly the boundary `AGENTS.md:18` warns not to smear. `references/` shallow clones are read-only (`AGENTS.md:28`), and verticals that reuse third-party layout (knowledge graph over `ForceLayout2D`, table over `Text`) record divergence notes at consumption sites (e.g. `editor.ts:117` cycle allowance, `FixedZLayout.ts:18` pin divergence). The website (`vectojs-website/`) consumes `vectojs-docs/content/` one-way via `scripts/sync-content.py` (`AGENTS.md:28` docs section), so a docs-only publish cannot outrun its authoritative source — the same discipline verticals need when their handbook lives beside `references/`.

## 7. Traps worth checking before you land a vertical change

1. **Borrowed-layout dispose** — `KnowledgeGraphModel.dispose()` must not dispose the borrowed layout (`KnowledgeGraphModel.ts:230`); `KnowledgeGraphSession.dispose()` must (`KnowledgeGraphSession.ts:277`). Swapping one for the other leaks or double-frees. `attach(scene)` at `KnowledgeGraphSession.ts:153` is idempotent and must be called before `render(renderer, scene)` at `KnowledgeGraphSession.ts:256` or it throws `call attach first`.
2. **Cursor staleness** — `MemoryDataSource` cursors version (`MemoryDataSource.ts:118`); `KnowledgeGraphModel.expand` dedup (`KnowledgeGraphModel.ts:134`) and post-await disposal bail (`KnowledgeGraphSession.ts:189`/`KnowledgeGraphSession.ts:227`) are the fences against mutating mid-pagination. `loadSnapshot` at `KnowledgeGraphSession.ts:202` marks every entity `complete` so select does not re-fetch already-present hops.
3. **Port direction + identity** — editor `validateLink` rejects same-node and wrong-direction ports (`model.ts:131`/`model.ts:138`); connection preview `isConnectionTarget` mirrors it via try-`addLink` (`editor.ts:360`/`editor.ts:549`). `findPortAt` at `editor.ts:502` walks reverse add-order so overlapping cards resolve to the topmost port, and `getLocalPoint` via `worldToLocal` at `editor.ts:498` keeps drag/hit in editor-local space, not `sceneX/Y`.
4. **Overlay a11y leak** — `WindowManager.focus`/`close` must `releaseA11yProjection` on blur (`WindowManager.ts:248`) and `DesktopWindow` must stay `pointerEvents none` (`Window.ts:400`) or the dialog mirror eats chrome clicks. Chrome buttons need `a11yProjection eager` (`Window.ts:383`) or the first click misses (next-frame `onDemand` projection), and `DisplayLayout.clampRect` (`DisplayLayout.ts:89`) must be used for every placement (`WindowManager.ts:149`).
5. **Table viewport math** — `reconcileVirtualRows`/`_syncGridA11y` share the same `first/last` bound (`Table.ts:397`/`Table.ts:662`) with `overscan`; `clampScrollPosition` at `Table.ts:344` keeps the spring offset from overshooting the clamped target; `rowTops` prefix at `Table.ts:751` keeps classic a11y `O(rows)` not `O(rows²)`. `pageRows()` at `Table.ts:548` uses mean `rowHeights` in classic mode and `viewportHeight - headerHeight` in virtualized, so `PageUp/PageDown` stay viewport-sized in both.
6. **Publishing runtime deps** — framework deps belong in `peerDependencies` + `workspace:*` in `devDependencies` (`app-level-and-toolchain.md:467`); `npm publish` ships `dependencies` verbatim (`app-level-and-toolchain.md:473`). Verify via a tarball-consumer probe (`bun add` in a scratch dir) before a forge app consumes a new minor.
7. **Selection + history coupling** — `deleteNodes` at `editor.ts:390` ends transient gestures first and batches nodes+incident links as one undoable command; `cancelConnection`/`cancelDrag` before `history.execute` prevents dangling `dragDocument` or pending connections. `applyAutoLayout` at `editor.ts:274` pushes only when the JSON changed, so a no-op layout does not pollute the undo stack.
8. **Persistence round-trip** — `exportDocument` at `persistence.ts:168` validates structurally _and_ semantically (`validateDocument` at `persistence.ts:84` with per-link `validateLink` at `persistence.ts:156`); `importDocument` at `persistence.ts:178` rejects wrong `schemaVersion` (`persistence.ts:186`). Bypassing `exportDocument` to hand-write JSON that passes only the structural half can still fail `validateLink` for `target-port-occupied` or `incompatible-types` on re-import — always round-trip through the same validator.

## 7a. Cross-vertical patterns worth stealing

Four verticals, four answers to the same question — "how does incremental growth stay cheap and reversible?":

- **Knowledge Graph: borrow, don't clone.** `KnowledgeGraphModel` never copies a layout — it borrows one (`KnowledgeGraphModel.ts:43` `layout?: GraphLayout` doc, "Borrowed, not owned"), drives it with `setGraph` + `reheat`, and leaves disposal to the session (`KnowledgeGraphSession.ts:277`). Any new vertical that wraps a shared engine (physics, text shaping, media decode) should copy this split: the model's `dispose` at `KnowledgeGraphModel.ts:225` is six lines of map/set clears with an explicit comment "leaves it untouched so it can stay shared with a live session" (`KnowledgeGraphModel.ts:230`). Forgetting the comment is how a future editor re-adds `this.layout?.dispose()` and breaks the session.

- **Node Editor: history owns the document.** Every mutation — `createLink` (`editor.ts:380` → `addLink` + `history.execute`), `deleteNodes` (`editor.ts:390` → batched `removeNode` + one `history.execute`), `moveDrag` (`editor.ts:441` → `history.execute('Move node', …)`), `applyAutoLayout` (`editor.ts:274` → `history.execute('Auto-layout', …)`) — goes through `CommandHistory.execute` (`history.ts:28`) which snapshots `before`/`after` via `cloneDocument` and clears `redoStack`. The editor never mutates `documentState` without going through `applyDocument` (`editor.ts:520` → `cloneDocument` + `rebuild` + `markDirty`). Steal this for any future canvas editor: one gate, one clone discipline, no out-of-band `documentState = …`.

- **Desktop: overlays own focus, not the scene.** `WindowManager` never calls `Entity.remove` to restack (`WindowManager.ts:339` `kids.splice` + `markStructureChanged`) because `remove` detaches a11y and unregisters drivers — thrashing on every focus change. It tracks focus via `focused` + `releaseA11yProjection`/`requestA11yProjection` (`WindowManager.ts:248`/`WindowManager.ts:253`) so `onDemand` background windows do not keep a permanent a11y mirror. The desktop `Wallpaper` is `a11yProjection never` (`DesktopShell.ts:26`), `DesktopWindow` is `onDemand` with `pointerEvents none` on its dialog mirror (`Window.ts:400`), and `Taskbar` is `eager` because its buttons must be reachable without a hover (`Taskbar.ts:58`). Any new shell surface should pick one of these three modes deliberately, not default to `eager`.

- **Table: window, pool, integrate.** Virtualization is three cooperating budgets: `reconcileVirtualRows` windows exactly `overscan`-padded `[first, last]` (`Table.ts:397`/`Table.ts:404`), `_syncGridA11y` pools `RowHotspot`/`GridCellHotspot` to `need` (`Table.ts:691`), and `update(dt)` integrates `scrollY` with a dt-aware spring (`Table.ts:352` `velY += diff*7.2*(dt/1000)` / `exp(-dt/84)`). `hasPendingAnimations` at `Table.ts:378` is the idle-throttle handshake that makes all three survive `renderMode onDemand`. A future virtualized list that copies only the window step but forgets `hasPendingAnimations` will scroll correctly once and then stall — the same bug `StreamReader` had at `app-level-and-toolchain.md:222`.

## 7b. Verification — how to prove a vertical change is correct

Verticals are app-shaped, so the proof is app-shaped too — not just unit tests.

- **Knowledge graph:** `just test-pkg knowledge-graph` covers `MemoryDataSource` cursor versioning (`MemoryDataSource.ts:118` stale-cursor throw) and `KnowledgeGraphModel` dedup/reheat (`KnowledgeGraphModel.ts:134`/`:285`); add a repro that calls `model.expand(id)` twice concurrently and asserts one promise, then `cancelExpand` and assert `cancelled` state. For rendering, drive `KnowledgeGraphSession` in a headed browser (not `jsdom`) and assert `graph.group.children.length` after `syncFromModel` — `Graph3D` is Three.js and needs a real canvas.

- **Node editor:** `just test-pkg node-editor` exercises `validateLink` error codes (`model.ts:36` `LinkValidationError`) and `exportDocument`/`importDocument` round-trip (`persistence.ts:168`); add a case that at-capacity `maxConnections` passes when the link is its own occupant (`persistence.ts:156` strip-self logic) and a drag case that asserts `history.canUndo` stays false after a no-op drag (`editor.ts:445`).

- **Desktop:** `just test-pkg desktop` covers `AppRegistry` and `DisplayLayout.clampRect` (`DisplayLayout.ts:89`); add a dialog-modality case that calls `wm.openDialog({modal:true})` then `wm.focus(other)` and asserts focus stays on the dialog (`WindowManager.ts:237` topModal gate), and a `restack` case that asserts `overlayRoot.children` order without `Entity.remove` (`WindowManager.ts:339`).

- **Table:** `just test-pkg table` covers `normalizeColumnWidths` (`Table.ts:789`) and `seenCells` duplicate rejection (`Table.ts:228`); add a virtualized case that sets `viewportHeight`, calls `appendRows` 10k times, scrolls to `maxScroll()` (`Table.ts:328`), and asserts `mountedRows.size` is `O(viewport)` not `O(rows)` (`Table.ts:392` window), plus a roving-tab case that asserts `isGridTabStop` re-anchors after the focused row scrolls out (`Table.ts:668`).

In every case run `just check` (oxfmt + oxlint + markdownlint) before pushing — vertical docs live beside code and share the same gate (`AGENTS.md:31` quality gates).

## 8. Checklist — before you land a vertical change

1. **Own your disposal.** `KnowledgeGraphModel` borrows, `KnowledgeGraphSession` owns (`KnowledgeGraphModel.ts:230` vs `KnowledgeGraphSession.ts:277`); `DesktopShell.dispose` at `DesktopShell.ts:251` tears down `shortcuts`/`windowManager`/`taskbar`/`wallpaper` in order. Grep every `dispose` you touch for borrowed vs owned.
2. **Guard every async continuation.** `bootstrap`/`expand` both bail on `this.disposed` post-await (`KnowledgeGraphSession.ts:189`/`KnowledgeGraphSession.ts:227`); `expand` dedups by id (`KnowledgeGraphModel.ts:134`) and `cancelExpand` aborts (`KnowledgeGraphModel.ts:150`). A new `await` without a guard reintroduces the late-mirror bug.
3. **Project a11y structurally.** `Table` is `role grid` with `RowHotspot` rows (`Table.ts:55`) and `GridCellHotspot` cells (`Table.ts:82`) pooled and `pointerEvents none`; `DesktopWindow` is `role dialog` with `pointerEvents none` (`Window.ts:400`) and eager chrome buttons (`Window.ts:383`); `NodeEditor` ports are `role button` with keyboard provenance (`editor.ts:90`). A new interactive child without `pointerEvents none` on its structural parent steals hits.
4. **Keep `hasPendingAnimations` honest.** `Table` at `Table.ts:378` and `StreamReader` after the fix at `app-level-and-toolchain.md:259` both return `true` while animating so `onDemand` never idles mid-gesture. Any new `update(dt)` that calls `markDirty` internally needs the same override.
5. **Validate before you publish.** `packages/desktop` broke on `workspace:*` in `dependencies` (`app-level-and-toolchain.md:455`); `packages/tex` broke on `sideEffects: false` (`app-level-and-toolchain.md:407`). Both survived CI because `vitest` aliases `src/` and no consumer probe ran. Add a tarball `bun add` smoke test to the release checklist.
6. **Record the finding in the right forge bucket.** `app-level-and-toolchain.md:1` is append-only (`app-level-and-toolchain.md:5`) — never edit an existing entry, never file an app trap under `core-*`/`simulation-*` (`forge/findings/README.md` classification). The split is what keeps engine triage from drowning in app noise, and the dogfooding loop (`vectojs-native/*/AGENTS.md`) depends on it.

## 9. Future verticals and extension points

New verticals should start from the seam that already exists, not from a fork.

- **Knowledge graph extensions.** Swap `KgDataSource` for a worker-backed or streaming source (`types.ts:54` `MaybeAsync<T>` at `types.ts:84` already allows sync or async): page size, `direction: 'out'|'in'|'both'` (`types.ts:64`), and `signal` abort are the only knobs the model needs. A timeline or map vertical reuses `KnowledgeGraphModel` with a different `GraphLayout` (e.g. a 1D temporal `FixedZLayout` variant or a `ForceLayout2D` projection) and a different `Graph3D` style — `GraphLayout` + `GraphData` are the contract, not the force law.

- **Node editor extensions.** `PortDefinition.dataType` (`model.ts:12`) + `maxConnections` (`model.ts:13`) already model typed sockets; `layoutDocument` options (`layout.ts:3` `originX/Y`, `horizontalGap`/`verticalGap`) are the only layout knobs. A shader-graph or audio-graph vertical adds `NodeData.type` branches and `validateLink` `incompatible-types` rules (`model.ts:140`) without touching `history.ts` or `selection.ts`. `CommandHistory` is intentionally label-bearing (`history.ts:4` `label`) so an undo log is human-readable.

- **Desktop extensions.** `AppRegistry.register` at `AppRegistry.ts:15` is the plugin seam (runtime registration without touching `WindowManager`), and `openDialog` at `WindowManager.ts:184` is the transient seam. A new app needs only `AppDefinition.create: (ctx: AppContext) => Entity` (`types.ts:1`) — `ctx` carries `scene`, `vfs`, `windowManager`, `close`, `appId`, `windowId`. `ShortcutRouter` (`ShortcutRouter.ts:1`) already namespaces `custom` actions so an app can add chords without colliding with `open-app`/`close-focused`.

- **Table extensions.** `Table` is already generic over `TableCell = string | Entity` (`Table.ts:4`), so a chart-cell or sparkline vertical passes an `Entity` cell and owns its own `width/height` — `fitCell`/`setCellSelectable` probe by capability (`Table.ts:5`), not by type tag. Virtualization stays `O(viewport)` as long as new cells stay sized through `setMaxWidth` (`Table.ts:6`). `appendRows` (`Table.ts:885`) is the only mutation — a `setRows` would need a11y re-anchoring and `seenCells` invalidation, which is why it is deliberately absent (`Table.ts:870` doc).

In every case the cost of a new vertical is one new package plus a `GraphData`/`NodeDocument`/`AppDefinition`/`TableCell` adapter — not a new engine.

## Appendix — where to read next

<!-- markdownlint-disable MD060 -->

| Goal                                | Start                                                                                     | Then                                                                                     |
| ----------------------------------- | ----------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------- |
| Page a knowledge graph              | `packages/knowledge-graph/src/KnowledgeGraphModel.ts:62` single driver                    | `KnowledgeGraphSession.ts:67` wiring → `packages/graph3d/src/Graph3D.ts:28` presentation |
| Keep settled nodes stable on expand | `KnowledgeGraphModel.ts:273` batch `loaded` + `KnowledgeGraphModel.ts:332` `rebuildGraph` | `FixedZLayout.ts:22` clamp → `GraphLayout.ts:12` contract                                |
| Add an editor command               | `packages/node-editor/src/history.ts:9` `execute` + `model.ts:126` `validateLink`         | `editor.ts:199` gesture → `persistence.ts:84` `validateDocument`                         |
| Open a desktop app or dialog        | `packages/desktop/src/WindowManager.ts:126` `open` / `:184` `openDialog`                  | `Window.ts:158` chrome → `DisplayLayout.ts:15` work areas → `AppRegistry.ts:1` catalog   |
| Export/import a document            | `packages/node-editor/src/persistence.ts:168` `exportDocument`                            | `persistence.ts:178` `importDocument` → `persistence.ts:84` `validateDocument`           |
| Add a desktop shortcut              | `packages/desktop/src/ShortcutRouter.ts:1` `ShortcutRouter`                               | `DesktopShell.ts:344` `dispatchShortcut` → `types.ts:1` `ShortcutAction`                 |
| Virtualize a table past 10k rows    | `packages/table/src/Table.ts:144` `viewportHeight`                                        | `Table.ts:392` `reconcileVirtualRows` → `Table.ts:624` pool → `Table.ts:352` integrator  |
| Classify a new finding              | `vectojs-docs/forge/findings/app-level-and-toolchain.md:1` header                         | `forge/findings/README.md` template → correct bucket, `Upstream status` line             |

> **Reading the seams:** the graph seam is `GraphData` (`graph3d/types.ts`), the editor seam is `NodeDocument` + `validateLink` (`model.ts:54`/`:126`), the desktop seam is `AppDefinition` + `AppContext` (`types.ts:1`), the table seam is `TableCell` (`Table.ts:4`). A new vertical that cannot name its seam in one type has not found its boundary yet.

---

_Series: 00 Overview → 01 Selection → 02 Text+Layout → 03 Projection+Virtualization → 04 Streaming Markdown → 05 TeX → 06 VMT Runtime → 07 Renderer → 08 WASM → 09 Three/XR → 10 Video Export → 11 Graph Layout → 12 DevTools → **15 Vertical Apps** → 99 Synthesis._
